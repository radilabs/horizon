# Factory runtime artifacts
reports/*
!reports/README.md

# Credential and secret ignore rules for Horizon
.env
.env.*
*.token
*.secret
*.oasis
auth.json
credentials.json
secrets/
oasis-token*

# Local collector / runtime state that must never be committed
*.pem
*.key
id_rsa
id_ed25519

__pycache__/
*.pyc

# Cursor / Electron local session artifacts (never commit)
state.vscdb
state.vscdb-*
Cookies
Cookies-journal
storage.json
